# Optional: override proxy target. Defaults to https://run.rfoel.dev.
# Use raw API Gateway URL if you want to skip CloudFront (faster but needs
# rewrite — keep default unless you know why).
# VITE_API_PROXY_TARGET=https://xxx.execute-api.us-east-1.amazonaws.com
